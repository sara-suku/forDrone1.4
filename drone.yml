kind: pipeline
name: default

platform:
  arch: amd64
  os: linux

pipeline:
  test:
    image: alpine/git
    image: java:8-jdk
    pull: true
    commands:
      - java -version      

services:  
  - name: seltest
    selenium:
          image: selenium/standalone-chrome:3.141.59-vanadium 
