kind: pipeline
type: docker
name: linux-amd64

platform:
  arch: amd64
  os: linux

steps:

  -  name: test
     image: alpine/git
     pull: true
     
  -  name: test123
     commands:
       - wget https://github.com/sormuras/bach/raw/master/install-jdk.sh
       - ls
when:
    event:
      - push
      - tag
